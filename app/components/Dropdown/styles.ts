import React from 'react';
import { TextInput } from 'react-native';
import styled from 'styled-components/native';
import { SelectList } from 'react-native-dropdown-select-list';

const Dropdown = styled(SelectList)`
  color: white;
`;

export default {
  Dropdown,
};
